$(document).ready(() => {
  const slider = new Swiper('.swiper-container', {
    loop: true,
    direction: 'vertical',
    speed: 1300,
    autoplay: 1500,
    effect: 'slide',
    spaceBetween: 40
  });
});
